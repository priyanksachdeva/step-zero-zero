<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===================== PERMISSIONS (Ordered & Annotated) ===================== -->
    <!-- Network access for API calls / analytics -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Physical activity recognition (native step APIs / classification) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Foreground service base permission -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Foreground health service (API 34+) for continuous step tracking -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <!-- Post notifications (Android 13+) so user can manage persistent tracking notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Camera for heart rate (PPG) measurement using rear camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Haptic feedback for subtle UI vibrations -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Health Connect app query so we can detect availability -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <!-- ===================== HARDWARE FEATURES ===================== -->
    <!-- Accelerometer required for step detection -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
    <!-- Camera required for heart rate feature -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <!-- Flash optional (improves PPG quality but not mandatory) -->
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

    <!-- ===================== APPLICATION ===================== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Foreground step tracking service scaffold -->
        <service
            android:name=".StepTrackingService"
            android:exported="false"
            android:foregroundServiceType="health|dataSync" />

    </application>
</manifest>
